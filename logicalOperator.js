// Here is the example of AND(&&) operator where both the side must be true or false:

var x = 3;
var y = 5;

console.log((x !=5) && (y > x));
console.log((x == y) && (x != y));

// Now next operator is OR(||) where both side must be differ:

console.log((x > y) || (y > x));
console.log((x < y) || (y > x));

// Here is ! opeartor which turns thr value in oppostte:

console.log(!x < y);
console.log(!x > y);